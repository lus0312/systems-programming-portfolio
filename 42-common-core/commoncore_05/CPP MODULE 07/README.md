# CPP Module 07 - 템플릿

C++98 모듈 과제입니다. 이번 모듈의 핵심: 함수 템플릿과 클래스 템플릿.

| 연습 | 내용 |
|---|---|
| ex00 | `swap`, `min`, `max` 함수 템플릿 |
| ex01 | `iter`: 배열의 각 원소에 함수 적용 |
| ex02 | `Array<T>`: 깊은 복사와 범위 검사를 갖춘 클래스 템플릿 |

## 빌드

각 `exNN` 폴더에서 `make`를 실행합니다 (`-Wall -Wextra -Werror -std=c++98`).
