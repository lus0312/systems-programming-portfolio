# CPP Module 08 - 템플릿 컨테이너와 반복자

C++98 모듈 과제입니다. 이번 모듈의 핵심: STL 컨테이너, 반복자, 알고리즘.

| 연습 | 내용 |
|---|---|
| ex00 | `easyfind`: 컨테이너에서 값 탐색 |
| ex01 | `Span`: 최단/최장 간격 계산 |
| ex02 | `MutantStack`: `std::stack`에 반복자를 추가 |

## 빌드

각 `exNN` 폴더에서 `make`를 실행합니다 (`-Wall -Wextra -Werror -std=c++98`).
