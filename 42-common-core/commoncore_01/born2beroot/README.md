# Born2beRoot

가상 머신 위에 리눅스 서버를 직접 설치하고 보안 정책을 적용하는 시스템 관리 입문 과제입니다.

이 폴더에는 코드가 없고, 제출물인 `signature.txt`(가상 디스크 이미지의 SHA-1 해시)만 있습니다.

## 과제에서 다루는 내용

- 디스크 파티셔닝(LVM, 암호화)
- SSH 설정과 포트 변경, UFW 방화벽
- `sudo` 정책, 사용자·그룹 관리, 비밀번호 정책
- `cron`과 셸 스크립트로 시스템 정보를 주기적으로 출력하는 `monitoring.sh`
