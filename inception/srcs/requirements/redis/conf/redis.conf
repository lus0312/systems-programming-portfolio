# Listen on all container interfaces so WordPress can connect over Docker network
bind 0.0.0.0

# Redis is only exposed inside the private Docker network
protected-mode no

# Default Redis service port
port 6379

# Keep Redis in the foreground for Docker
daemonize no
